SYMPTOM_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文，从“主诉/现病史”中抽取信息，并输出符合指定 JSON Schema 的 JSON（只输出 JSON，不要输出任何解释或多余文本）。

## 主诉与现病史
{symptom_text}

## 抽取字段（全部使用中文）
1) chief_complaint（主诉）
- 优先取“主诉：”后的完整表述。
- 如果文本中没有明确“主诉”标签，则从现病史第一句中概括“就诊最主要原因”，但不得臆造。

2) additional_symptom（伴随症状/补充症状）
- 包含：主诉以外的阳性症状（如咳嗽、咳痰、乏力等）与重要阴性症状（如“无发热、无咯血”等）。
- 以简洁条目串联，建议用“；”分隔，保持原文措辞优先。

3) symptom_duration（症状持续时间）
- 从文本中提取与主诉/现病史相关的持续时间表达，例如“1月余”“3天”“2周”等。
- 若出现多个时间，以**与主诉最直接相关**的那个为主；无法判断则保留最早出现且最明确的时间表达。
- 原文怎么写就怎么写，不要擅自换算为天数；缺失则置为空字符串 ""。

## 输出要求（非常重要）
- 只输出一个 JSON 对象，键名必须严格为：chief_complaint、additional_symptom、symptom_duration
- 不能新增字段，不能输出 markdown，不能输出解释。
- 所有字段必须存在；缺失信息用空字符串 ""。
"""

PERSONAL_HISTORY_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文，从“个人史”内容中抽取信息，并输出符合指定 JSON Schema 的 JSON（只输出 JSON，不要输出任何解释或多余文本）。

## 个人史
{personal_history_text}

## 抽取字段（全部使用中文）
1) smoking_status（吸烟史描述）
- 抽取与吸烟相关的原文描述，优先保留强度与年限（如“45年，1包/天，已戒烟1周”）。
- 若文本明确“否认吸烟史/从不吸烟”，则写为对应原文表述。
- 若缺失，填空字符串 ""（不要编造）。

2) alcohol_use（饮酒史描述）
- 抽取与饮酒相关的原文描述（如“否认饮酒史”“偶尔饮酒20年”）。
- 若缺失，填空字符串 ""（不要编造）。

## 输出要求（非常重要）
- 只输出一个 JSON 对象，键名必须严格为：smoking_status、alcohol_use
- 不能新增字段，不能输出 markdown，不能输出解释。
- 所有字段必须存在；缺失信息用空字符串 ""。
"""


AUXILIARY_EXAMINATION_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文，从“辅助检查/影像学/病理/检验/基因检测”等内容中抽取检查条目，并输出符合指定 JSON Schema 的 JSON（只输出 JSON，不要输出任何解释或多余文本）。

## 辅助检查
{auxiliary_examination_text}

## 抽取目标（全部使用中文）
输出一个对象，包含字段：
- auxiliary_examination：检查条目列表（按原文出现顺序）

每个检查条目为一个对象，必须包含：
1) check_type：检查类型
- 从原文识别并规范为简短类型，如：CT、增强CT、MRI、PET-CT、超声、X线、血液检查、病理、免疫组化、基因检测 等。
- 若原文写“胸部增强CT”，check_type 可写“增强CT”（或“CT增强”也可，但请保持一致）。

2) item：检查项目/部位/取材等
- 提取检查的部位或项目名称，如“胸部”“右肺门”“颈部淋巴结穿刺活检”等。
- 如果原文把部位和项目写在一起，也可以整体放入 item（尽量简洁）。

3) result：检查结果原文/描述
- 放入该项检查对应的结果描述，尽量保留原文（可适度去掉多余空白）。
- 不要把“考虑/提示/待排”等判断删掉；它们属于结果的一部分。

## 规则（非常重要）
- 只抽取文本中明确出现的检查；不要臆造检查或结果。
- 若某条检查缺少某个字段信息：该字段填空字符串 ""（但条目仍保留）。
- 保持列表顺序与原文一致；同一天同类型多次检查也要分成多条。
- 只输出 JSON；不能输出 markdown、解释或多余文本。
- 键名必须严格匹配 Schema：auxiliary_examination / check_type / item / result，不能新增字段。
"""

AUXILIARY_EXAMINATION_EXTRA_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文和已有的辅助检查 JSON，从“辅助检查/影像学/病理/检验/基因检测”等内容中补充遗漏的检查条目，并输出符合指定 JSON Schema 的 JSON（只输出 JSON，不要输出任何解释或多余文本）。

## 扼要病情及治疗经过
{auxiliary_examination_text}

## 已知辅助检查（JSON 格式）
{auxiliary_examination_existing}

## 抽取目标（全部使用中文）
输出一个对象，包含字段：
- auxiliary_examination：检查条目列表（按原文出现顺序）

每个检查条目为一个对象，必须包含：
1) check_type：检查类型
- 从原文识别并规范为简短类型，如：CT、增强CT、MRI、PET-CT、超声、X线、血液检查、病理、免疫组化、基因检测 等。
- 若原文写“胸部增强CT”，check_type 可写“增强CT”（或“CT增强”也可，但请保持一致）。

2) item：检查项目/部位/取材等
- 提取检查的部位或项目名称，如“胸部”“右肺门”“颈部淋巴结穿刺活检”等。
- 如果原文把部位和项目写在一起，也可以整体放入 item（尽量简洁）。

3) result：检查结果原文/描述
- 放入该项检查对应的结果描述，尽量保留原文（可适度去掉多余空白）。
- 不要把“考虑/提示/待排”等判断删掉；它们属于结果的一部分。

## 规则（非常重要）
- 只抽取文本中明确出现的检查；不要臆造检查或结果。
- 若某条检查缺少某个字段信息：该字段填空字符串 ""（但条目仍保留）。
- 保持列表顺序与原文一致；同一天同类型多次检查也要分成多条。
- 只输出 JSON；不能输出 markdown、解释或多余文本。
- 键名必须严格匹配 Schema：auxiliary_examination / check_type / item / result，不能新增字段。
- **如果在已知辅助检查中已经包含了某个检查条目，则不需要重复添加该条目。**
"""


DIAGNOSIS_PROMPT = """
你是一名专业的出院记录信息抽取与结构化专家。你要抽取的是【首次治疗开始时（第一次化疗/首次治疗决策时）】的诊断结论，而不是出院后的诊断总结。

## 核心规则（非常重要）
1) 诊断主来源必须是【扼要病情及治疗经过】中与“开始治疗/开始化疗/于XXXX开始予……”同一段落或紧邻段落的诊断表述，例如：
   - “患者诊断考虑：……”
   - “诊断考虑：……”
   - “考虑：……”
2) 【出院诊断】常包含“1周期化疗后/治疗后”等结果性措辞，不能直接当作首次治疗前诊断来源；只能用于核对是否一致。
3) 输出的 diagnosis 必须去掉“1周期后/治疗后”等后缀，只保留首次治疗时的诊断结论（例如“右肺小细胞癌cT1cN1M0 II期、乙肝小三阳”）。
4) 仅抽取原文明确出现的内容；不要补全、不要推断、不要改写分期分型。
5) 若无法从【扼要病情及治疗经过】中确定首次治疗时诊断（即找不到“诊断考虑/患者诊断考虑”等明确句），则 diagnosis 输出空字符串 ""。

## 输入文本
### 扼要病情及治疗经过（优先依据）
{auxiliary_examination_text}

### 出院诊断（供核对，不可作为主要来源）
{diagnosis_text}

### 出院医嘱（通常是后续安排，仅供参考）
{treatment_text}

## 输出要求（严格）
- 只输出一个 JSON 对象（不要输出 markdown、不要输出解释、不要输出多余文本）
- JSON 键名必须严格为 diagnosis，不能新增字段
"""


TREATMENT_PROMPT = """
你是一名专业的出院记录信息抽取与结构化专家。你要抽取的是【首次治疗计划/第一次化疗方案】（首次开始治疗时执行或决定的方案），不是出院后的随访与下一周期安排。

## 核心规则（非常重要）
1) 治疗方案主来源必须是【扼要病情及治疗经过】中明确表示“开始给予/于XXXX开始予……方案化疗/治疗”的那一句（或紧邻句）。
   - 优先抓取包含药物名 + 剂量 + 给药方式 + d1-3/疗程等信息的原文片段。
2) 【出院医嘱】通常包含“下一周期时间、随访、监测”等，不属于首次治疗方案；除非原文明确重复首次方案，否则不要从出院医嘱抽取方案细节。
3) treatment 只输出“首次治疗方案本身”，不要包含“下一周期时间、随访门诊、监测频率、电话联系床位”等内容。
4) 仅抽取原文明确出现的内容；不要补全剂量、不要推断疗程。
5) 若无法从【扼要病情及治疗经过】中确定首次治疗方案（找不到“于XXXX开始予/开始给予…方案”），则 treatment 输出空字符串 ""。

## 输入文本
### 扼要病情及治疗经过（优先依据）
{auxiliary_examination_text}

### 出院诊断（仅供参考）
{diagnosis_text}

### 出院医嘱（通常为后续计划，仅供参考）
{treatment_text}

## 输出要求（严格）
- 只输出一个 JSON 对象（不要输出 markdown、不要输出解释、不要输出多余文本）
- JSON 键名必须严格为 treatment，不能新增字段
"""

ADDITIONAL_INFO_PROMPT = """你是一名专业的病历信息抽取与结构化专家。请基于【病历原文】对【已提取结果】进行校正与补全，输出符合指定 JSON Schema 的 JSON。
注意：已提取结果可能不准确，你必须以“病历原文”为唯一事实依据；只允许在原文可支持的情况下修改/补充，不得臆造。

## 病历原文（唯一事实来源）
{input}

## 已提取结果（可能有误，仅供参考，可修改/补充）
### symptom（JSON 字符串）
{symptom}

### diagnosis（字符串）
{diagnosis}

### treatment（字符串）
{treatment}

### auxiliary_examination（JSON 字符串）
{auxiliary_examination}

## 关键时序要求（非常重要）
这是出院记录，文本里可能同时包含：
- 首次治疗开始前/开始治疗时的信息（例如“患者诊断考虑：……”“于XXXX开始予……方案化疗”）
- 出院时总结信息（例如“出院诊断：……1周期化疗后”“出院医嘱：下一周期时间……”）

本任务中：
1) diagnosis 必须是【首次治疗开始时/首次治疗决策时】的诊断结论：
   - 优先从“扼要病情及治疗经过”中含“患者诊断考虑/诊断考虑/考虑为……”且与“开始治疗/开始化疗”同段或相邻段的语句抽取。
   - 不要直接输出“出院诊断”中带“1周期后/治疗后”的表述；必须回溯到首次治疗时的诊断句。
   - 如果“入院情况”中指说明了“结节性质待查”之类的，那么需要结合“出院情况”中的结论来判断首次诊断内容。比如，出院时明确诊断为“右上肺癌(cT1cN0M0，术后病理未回) ”，那么首次诊断应当为“右上肺癌(cT1cN0M0) ”。
2) treatment 必须是【首次治疗计划/第一次化疗方案】：
   - 以“于XXXX开始予/开始给予……方案化疗”这一句为锚点，抽取药物名+剂量+给药方式+天数(d1-3等)+周期等原文。
   - 不要把“出院医嘱”的“下一周期时间/随访/监测”等当作首次治疗方案。

若无法从原文确定首次治疗时的 diagnosis 或 treatment，则对应字段输出空字符串 ""。

## 字段抽取/修正规则（全部中文）
你需要输出以下字段（键名必须严格一致）：

1) chief_complaint（主诉）
- 优先取“主诉：”后的完整表述；若无明确标签，可从现病史第一句概括就诊最主要原因，但不得臆造。

2) additional_symptom（伴随症状/补充症状）
- 收集主诉之外的阳性症状与重要阴性症状（如“无发热/无咯血”等）。
- 以简洁短语输出，建议用“；”分隔，尽量保留原文措辞。
- 若原文明示无相关症状，可写“无”或空字符串。

3) symptom_duration（症状持续时间）
- 抽取与主诉最直接相关的时间表达（如“1月余/2周/3天”等），原文怎么写就怎么写；缺失则 ""。

4) diagnosis（诊断结果内容）
- 按“关键时序要求”抽取首次治疗时诊断，保留分期/分型信息（如 cT1cN1M0 II期）与重要合并症（如 乙肝小三阳）。
- 多个诊断用“、”连接；不要包含“1周期后/治疗后”等字样。

5) treatment（治疗方案内容）
- 按“关键时序要求”抽取首次治疗方案原文（药物+剂量+途径+天数/周期等）。
- 不要包含“下一周期时间/随访门诊/监测频率/电话联系床位”等出院医嘱内容。

6) auxiliary_examination（辅助检查列表）
- 从原文抽取“影像/病理/检验/基因检测/心电图/骨显像/支气管镜”等检查条目，按原文出现顺序输出。
- 每条检查必须包含：
  - check_type：检查类型（如 CT、增强CT、MRI、心电图、骨显像、支气管镜、病理、免疫组化 等）
  - item：检查项目/部位/取材（如 胸部、颅脑、右肺门淋巴结穿刺 等）
  - result：结果描述（尽量保留原文判断，如“考虑…待排…”）
- 只抽取原文明确出现的检查，不要臆造；若某条检查缺少某字段，该字段填 ""。
- 若原文完全未提及任何检查，则输出空列表 []。

## 输出要求（严格）
- 只输出一个 JSON 对象（不要输出 markdown、不要输出解释、不要输出多余文本）
- 不能新增字段，不能少字段；字段必须全部出现
- 缺失信息用空字符串 ""；auxiliary_examination 缺失则 []
"""
