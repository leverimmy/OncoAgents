# MDT RAG Tool

>   Suppose the current folder is `path/to/OncoAgents`.

Run the following command to test the RAG tool:

```bash
uv run python3 -m src.mdt.rag_test \
    --seed 42 \
    --count 10
```
