SYMPTOM_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文，从“主诉/现病史”中抽取信息，并输出符合指定格式的 JSON。

## 主诉与现病史
{symptom_text}

## 抽取字段（全部使用中文）
1) chief_complaint（主诉）
- 优先取“主诉：”后的完整表述。
- 如果文本中没有明确“主诉”标签，则从现病史第一句中概括“就诊最主要原因”，但不得臆造。

2) additional_symptom（伴随症状/补充症状）
- 包含：主诉以外的阳性症状（如咳嗽、咳痰、乏力等）与重要阴性症状（如“无发热、无咯血”等）。
- 以简洁条目串联，建议用“；”分隔，保持原文措辞优先。

3) symptom_duration（症状持续时间）
- 从文本中提取与主诉/现病史相关的持续时间表达，例如“1月余”“3天”“2周”等。
- 若出现多个时间，以**与主诉最直接相关**的那个为主；无法判断则保留最早出现且最明确的时间表达。
- 原文怎么写就怎么写，不要擅自换算为天数；缺失则置为"未知"。

## 输出格式（必须严格遵守）

请严格按照以下 JSON 格式输出（不要输出 markdown、解释或多余文本）：

{{
   "chief_complaint": "主诉内容",
   "additional_symptom": "伴随症状内容",
   "symptom_duration": "症状持续时间内容"
}}
"""

PERSONAL_HISTORY_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文，从“个人史”内容中抽取信息，并输出符合指定格式的 JSON。

## 个人史
{personal_history_text}

## 抽取字段（全部使用中文）
1) smoking_status（吸烟史描述）
- 抽取与吸烟相关的原文描述，优先保留强度与年限（如“45年，1包/天，已戒烟1周”）。
- 若文本明确“否认吸烟史/从不吸烟”，则写为对应原文表述。
- 若缺失，填写 "未知"（不要编造）。

2) alcohol_use（饮酒史描述）
- 抽取与饮酒相关的原文描述（如“否认饮酒史”“偶尔饮酒20年”）。
- 若缺失，填写 "未知"（不要编造）。

## 输出格式（必须严格遵守）

请严格按照以下 JSON 格式输出（不要输出 markdown、解释或多余文本）：

{{
   "smoking_status": "吸烟史描述",
   "alcohol_use": "饮酒史描述"
}}
"""


AUXILIARY_EXAMINATION_PROMPT = """你是一名专业的病历信息抽取与结构化专家。你的任务是：仅根据提供的病历原文，从“辅助检查/影像学/病理/检验/基因检测”等内容中抽取检查条目，并输出符合指定格式的 JSON。

## 辅助检查
{auxiliary_examination_text}

## 抽取目标（全部使用中文）

输出一个 JSON 对象，包含字段：
- auxiliary_examination：检查条目列表（按原文出现顺序）

每个检查条目为一个对象，必须包含：
1) check_type：检查类型
- 从原文识别并规范为简短类型，如：CT、增强CT、MRI、PET-CT、超声、X线、血液检查、病理、免疫组化、基因检测 等。
- 若原文写“胸部增强CT”，check_type 可写“增强CT”（不要写“CT增强”）。
- 如果原文检查的是血常规、肝肾功能等检验类检查，check_type 可统一写为“检验”或“实验室检查”。

2) item：检查项目/部位/取材等
- 提取检查的部位或项目名称，如“胸部”“右肺门”“颈部淋巴结穿刺活检”等。
- 如果原文把部位和项目写在一起，也可以整体放入 item（尽量简洁）。
- 如果是一次检查包含多个部位或项目，可以用“；”分隔放在同一个 item 中。

3) result：检查结果原文/描述
- 放入该项检查对应的结果描述，尽量保留原文（可适度去掉多余空白）。
- 如果有“考虑/提示/待排”等判断，保留它们；它们属于结果的一部分。
- 如果同一个 check_type 同一个 item 包含多个 result，可以用“；”分隔放在同一个 result 中。

## 规则（非常重要）
- 只抽取文本中明确出现的检查；不要臆造检查或结果。
- 若某条检查缺少某个字段信息：该字段填空字符串 ""

## 输出格式（必须严格遵守）

请严格按照以下 JSON 格式输出（不要输出 markdown、解释或多余文本）：

{{
   "auxiliary_examination": [
      {{
         "check_type": "检查类型",
         "item": "检查项目/部位/取材",
         "result": "检查结果原文/描述"
      }},
      ...
   ]
}}
"""

ADDITIONAL_INFO_PROMPT = """你是一名专业的病历信息抽取与结构化专家。请基于【病历原文】对【已提取结果】进行校正与补全，并输出符合指定格式的 JSON。
注意：已提取结果可能不准确，你必须以“病历原文”为唯一事实依据；只允许在原文可支持的情况下修改/补充，不得臆造。

## 病历原文（唯一事实来源）
{text}

## 已提取结果（可能有误，仅供参考，可修改/补充）

### symptom（JSON 字符串）
{symptom}

### auxiliary_examination（JSON 字符串）
{auxiliary_examination}

## 关键时序要求（非常重要）

病历为出院记录，文本里可能同时包含：
- 首次治疗开始前/开始治疗时的信息（例如“患者诊断考虑：……”“于XXXX开始予……方案化疗”）
- 出院时总结信息（例如“出院诊断：……1周期化疗后”“出院医嘱：下一周期时间……”）

本任务中：
1) diagnosis 必须是出院情况的诊断结论：
   - 你需要结合“出院情况”中的结论来判断首次诊断内容。比如，出院时明确诊断为“右上肺癌(cT1cN0M0，术后病理未回) ”，那么首次诊断应当为“右上肺癌(cT1cN0M0) ”。
   - 优先从“扼要病情及治疗经过”中含“患者诊断考虑/诊断考虑/考虑为……”且与“开始治疗/开始化疗”同段或相邻段的语句抽取。
   - 不要直接输出“出院诊断”中带“化疗后/1周期后/治疗后”的表述，也不要输出“术后xxx”等表述；必须回溯到给出治疗前对应的诊断时的诊断句。
2) treatment 必须是【首次治疗计划/第一次化疗方案】：
   - 以“于XXXX开始予/开始给予……方案化疗”这一句为锚点，抽取药物名+剂量+给药方式+天数(d1-3等)+周期等原文。
   - 以“排除介入手术禁忌后，行…方案”为锚点，抽取具体手术类型和方案。
   - 不要把“出院医嘱”的“下一周期时间/随访/监测”等当作首次治疗方案。
   - 如果患者拒绝行治疗，则填写院方提供的“拟行……方案”内容。

若无法从原文确定首次治疗时的 diagnosis 或 treatment，则对应字段输出空字符串 ""。

## 字段抽取/修正规则（全部中文）

你需要输出以下字段（键名必须严格一致）：

1.  chief_complaint（主诉）
   - 如果文本中没有明确“主诉”标签，则从现病史第一句中概括“就诊最主要原因”，但不得臆造。

2.  additional_symptom（伴随症状/补充症状）
   - 包含：主诉以外的阳性症状（如咳嗽、咳痰、乏力等）与重要阴性症状（如“无发热、无咯血”等）。
   - 以简洁条目串联，建议用“；”分隔，保持原文措辞优先。

3.  symptom_duration（症状持续时间）
   - 从文本中提取与主诉/现病史相关的持续时间表达，例如“1月余”“3天”“2周”等。
   - 若出现多个时间，以**与主诉最直接相关**的那个为主；无法判断则保留最早出现且最明确的时间表达。
   - 原文怎么写就怎么写，不要擅自换算为天数；缺失则置为"未知"。

4.  diagnosis（诊断结果内容）
   - 抽取出院时诊断，保留分期/分型信息（如 cT1cN1M0 II期）与重要合并症（如 乙肝小三阳）。
   - 多个诊断用“、”连接；不要包含“1周期后/治疗后”等字样；不要包含“术后xxx”等字样。
   - 重点分为四部分：
      1) disease_name（疾病名称）：患者的主要诊断疾病名称，如“右肺小细胞癌”“甲状腺乳头状癌”等。
      2) stage（分期信息）：疾病准确的 TNM 分期描述，如“cT1cN1M0 II期”“”。
       - 如果文本中没有明确分期信息，不要擅自推断分期，该字段置空字符串 ""。
      3) complication（合并症，用“;”分隔多个）

5.  treatment（治疗方案内容）
   - 如果是手术，按“手术方案类型”为锚点，抽取具体手术类型和方案，如“右肺上叶癌根治术(肺叶切除术、肺内淋巴结清扫术、纵隔淋巴结清扫术)、胸膜粘连烙断术”。
   - 如果是化疗、药物治疗等，按“关键时序要求”抽取首次治疗方案原文（药物+剂量+途径+天数/周期等）。
   - 不要包含“下一周期时间/随访门诊/监测频率/电话联系床位”等出院医嘱内容。
   - 分为两部分：
      1) plan（治疗方案内容）：
         - 如果是手术，保留手术类型，如“右肺上叶癌根治术(肺叶切除术、肺内淋巴结清扫术、纵隔淋巴结清扫术)、胸膜粘连烙断术”
         - 如果是化疗等，则保留药物名、剂量、给药方式等信息，比如“依托泊苷 125mg ivgtt d1-3+ 顺铂 40mg ivgtt d1-3方案化疗1周期”
         - 如果是开口服药物治疗，则写“继续口服药物治疗”等。
      2) course（治疗周期信息，如d1-3/q3w/qd/bid/tid等）：
         - 治疗需要多长时间、多长周期，若文本中没有明确描述该信息，则置空字符串 ""。
         - **请用自然语言描述，如“每3周1次，共6周期”“每三天一次”“每天一次”“每日两次”等**。

6.  auxiliary_examination（辅助检查列表）
   每个检查条目为一个对象，必须包含：
   1) check_type：检查类型
   - 从原文识别并规范为简短类型，如：CT、增强CT、MRI、PET-CT、超声、X线、血液检查、病理、免疫组化、基因检测 等。
   - 若原文写“胸部增强CT”，check_type 可写“增强CT”（不要写“CT增强”）。
   - 如果原文检查的是血常规、肝肾功能等检验类检查，check_type 可统一写为“检验”或“实验室检查”。

   2) item：检查项目/部位/取材等
   - 提取检查的部位或项目名称，如“胸部”“右肺门”“颈部淋巴结穿刺活检”等。
   - 如果原文把部位和项目写在一起，也可以整体放入 item（尽量简洁）。
   - 如果是一次检查包含多个部位或项目，可以用“；”分隔放在同一个 item 中。

   3) result：检查结果原文/描述
   - 放入该项检查对应的结果描述，尽量保留原文（可适度去掉多余空白）。
   - 如果有“考虑/提示/待排”等判断，保留它们；它们属于结果的一部分。
   - 如果同一个 check_type 同一个 item 包含多个 result，可以用“；”分隔放在同一个 result 中。

## 输出格式（必须严格遵守）

请严格按照以下 JSON 格式输出（不要输出 markdown、解释或多余文本）：

{{
   "chief_complaint": "主诉内容",
   "additional_symptom": "伴随症状内容",
   "symptom_duration": "症状持续时间内容",
   "diagnosis": {{
      "disease_name": "疾病名称",
      "stage": "分期信息",
      "complication": "重要合并症，用“;”分隔多个"
   }}
   "treatment": {{
      "plan": "治疗方案内容",
      "course": "治疗周期信息，用自然语言描述（如“每日两次”“每周三次”等）"
   }}
   "auxiliary_examination": [
      {{
         "check_type": "检查类型",
         "item": "检查项目/部位/取材",
         "result": "检查结果原文/描述"
      }},
      ...
   ]
}}
"""
