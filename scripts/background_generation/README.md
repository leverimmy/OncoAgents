# Patient Background Generation

>   Suppose the current folder is `path/to/OncoAgents/scripts/background_generation`.

Generate patients' background data:

```bash
uv run main.py \
    --output_dir ../../data/background
```
